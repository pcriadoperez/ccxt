package io.github.ccxt;

import io.github.ccxt.types.*;

import java.util.List;
import java.util.Map;
import java.util.concurrent.CompletableFuture;
import java.util.stream.Collectors;

// PLEASE DO NOT EDIT THIS FILE, IT IS GENERATED AND WILL BE OVERWRITTEN:
// https://github.com/ccxt/ccxt/blob/master/CONTRIBUTING.md#how-to-contribute-code

@SuppressWarnings("unchecked")
public class ExchangeTyped {

    private final Exchange exchange;

    public ExchangeTyped(Exchange exchange) {
        this.exchange = exchange;
    }

    public Exchange getExchange() {
        return exchange;
    }

    private static <T> List<T> toTypedList(Object raw, java.util.function.Function<Object, T> ctor) {
        return ((List<Object>) raw).stream().map(ctor).collect(Collectors.toList());
    }

    // --- LoadMarkets (special: first arg is Boolean reload, not params) ---
    public Map<String, MarketInterface> loadMarkets(boolean reload) {
        Object res = exchange.loadMarkets(reload).join();
        java.util.LinkedHashMap<String, MarketInterface> result = new java.util.LinkedHashMap<>();
        for (Map.Entry<String, Object> entry : ((Map<String, Object>) res).entrySet()) {
            result.put(entry.getKey(), new MarketInterface(entry.getValue()));
        }
        return result;
    }
    public Map<String, MarketInterface> loadMarkets() { return loadMarkets(false); }
    public CompletableFuture<Map<String, MarketInterface>> loadMarketsAsync(boolean reload) {
        return exchange.loadMarkets(reload).thenApply(res -> {
            java.util.LinkedHashMap<String, MarketInterface> result = new java.util.LinkedHashMap<>();
            for (Map.Entry<String, Object> entry : ((Map<String, Object>) res).entrySet()) {
                result.put(entry.getKey(), new MarketInterface(entry.getValue()));
            }
            return result;
        });
    }
    public CompletableFuture<Map<String, MarketInterface>> loadMarketsAsync() { return loadMarketsAsync(false); }

    // ==========================================
    // Market Data
    // ==========================================

    public Currencies fetchCurrencies(Map<String, Object> params) {
        Object res = exchange.fetchCurrencies(params).join();
        return new Currencies(res);
    }
    public Currencies fetchCurrencies() { return fetchCurrencies(null); }
    public CompletableFuture<Currencies> fetchCurrenciesAsync(Map<String, Object> params) {
        return exchange.fetchCurrencies(params).thenApply(Currencies::new);
    }

    public List<MarketInterface> fetchMarkets(Map<String, Object> params) {
        Object res = exchange.fetchMarkets(params).join();
        return toTypedList(res, MarketInterface::new);
    }
    public List<MarketInterface> fetchMarkets() { return fetchMarkets(null); }
    public CompletableFuture<List<MarketInterface>> fetchMarketsAsync(Map<String, Object> params) {
        return exchange.fetchMarkets(params).thenApply(res -> toTypedList(res, MarketInterface::new));
    }

    public List<Trade> fetchTrades(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchTrades(symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> fetchTrades(String symbol) { return fetchTrades(symbol, null, null, null); }
    public CompletableFuture<List<Trade>> fetchTradesAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchTrades(symbol, since, limit, params).thenApply(res -> toTypedList(res, Trade::new));
    }

    public List<Trade> fetchTradesWs(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchTradesWs(symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> fetchTradesWs(String symbol) { return fetchTradesWs(symbol, null, null, null); }
    public CompletableFuture<List<Trade>> fetchTradesWsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchTradesWs(symbol, since, limit, params).thenApply(res -> toTypedList(res, Trade::new));
    }

    public OrderBook fetchOrderBook(String symbol, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOrderBook(symbol, limit, params).join();
        return new OrderBook(res);
    }
    public OrderBook fetchOrderBook(String symbol) { return fetchOrderBook(symbol, null, null); }
    public CompletableFuture<OrderBook> fetchOrderBookAsync(String symbol, Long limit, Map<String, Object> params) {
        return exchange.fetchOrderBook(symbol, limit, params).thenApply(OrderBook::new);
    }

    public OrderBook fetchOrderBookWs(String symbol, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOrderBookWs(symbol, limit, params).join();
        return new OrderBook(res);
    }
    public OrderBook fetchOrderBookWs(String symbol) { return fetchOrderBookWs(symbol, null, null); }
    public CompletableFuture<OrderBook> fetchOrderBookWsAsync(String symbol, Long limit, Map<String, Object> params) {
        return exchange.fetchOrderBookWs(symbol, limit, params).thenApply(OrderBook::new);
    }

    public Long fetchTime(Map<String, Object> params) {
        Object res = exchange.fetchTime(params).join();
        return (res instanceof Number n) ? n.longValue() : null;
    }
    public Long fetchTime() { return fetchTime(null); }
    public CompletableFuture<Long> fetchTimeAsync(Map<String, Object> params) {
        return exchange.fetchTime(params).thenApply(res -> (res instanceof Number n) ? n.longValue() : null);
    }

    public List<OHLCV> fetchOHLCV(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOHLCV(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OHLCV::new);
    }
    public List<OHLCV> fetchOHLCV(String symbol) { return fetchOHLCV(symbol, "1m", null, null, null); }
    public CompletableFuture<List<OHLCV>> fetchOHLCVAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOHLCV(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, OHLCV::new));
    }

    public List<OHLCV> fetchOHLCVWs(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOHLCVWs(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OHLCV::new);
    }
    public List<OHLCV> fetchOHLCVWs(String symbol) { return fetchOHLCVWs(symbol, "1m", null, null, null); }
    public CompletableFuture<List<OHLCV>> fetchOHLCVWsAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOHLCVWs(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, OHLCV::new));
    }

    public Tickers fetchBidsAsks(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchBidsAsks(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers fetchBidsAsks() { return fetchBidsAsks(null, null); }
    public CompletableFuture<Tickers> fetchBidsAsksAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchBidsAsks(symbols, params).thenApply(Tickers::new);
    }

    public Ticker fetchTicker(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchTicker(symbol, params).join();
        return new Ticker(res);
    }
    public Ticker fetchTicker(String symbol) { return fetchTicker(symbol, null); }
    public CompletableFuture<Ticker> fetchTickerAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchTicker(symbol, params).thenApply(Ticker::new);
    }

    public Ticker fetchMarkPrice(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchMarkPrice(symbol, params).join();
        return new Ticker(res);
    }
    public Ticker fetchMarkPrice(String symbol) { return fetchMarkPrice(symbol, null); }
    public CompletableFuture<Ticker> fetchMarkPriceAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchMarkPrice(symbol, params).thenApply(Ticker::new);
    }

    public Ticker fetchTickerWs(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchTickerWs(symbol, params).join();
        return new Ticker(res);
    }
    public Ticker fetchTickerWs(String symbol) { return fetchTickerWs(symbol, null); }
    public CompletableFuture<Ticker> fetchTickerWsAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchTickerWs(symbol, params).thenApply(Ticker::new);
    }

    public Tickers fetchTickers(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchTickers(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers fetchTickers() { return fetchTickers(null, null); }
    public CompletableFuture<Tickers> fetchTickersAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchTickers(symbols, params).thenApply(Tickers::new);
    }

    public Tickers fetchMarkPrices(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchMarkPrices(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers fetchMarkPrices() { return fetchMarkPrices(null, null); }
    public CompletableFuture<Tickers> fetchMarkPricesAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchMarkPrices(symbols, params).thenApply(Tickers::new);
    }

    public Tickers fetchTickersWs(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchTickersWs(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers fetchTickersWs() { return fetchTickersWs(null, null); }
    public CompletableFuture<Tickers> fetchTickersWsAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchTickersWs(symbols, params).thenApply(Tickers::new);
    }

    public List<LeverageTier> fetchMarketLeverageTiers(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchMarketLeverageTiers(symbol, params).join();
        return toTypedList(res, LeverageTier::new);
    }
    public List<LeverageTier> fetchMarketLeverageTiers(String symbol) { return fetchMarketLeverageTiers(symbol, null); }
    public CompletableFuture<List<LeverageTier>> fetchMarketLeverageTiersAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchMarketLeverageTiers(symbol, params).thenApply(res -> toTypedList(res, LeverageTier::new));
    }

    // ==========================================
    // Account
    // ==========================================

    public List<Account> fetchAccounts(Map<String, Object> params) {
        Object res = exchange.fetchAccounts(params).join();
        return toTypedList(res, Account::new);
    }
    public List<Account> fetchAccounts() { return fetchAccounts(null); }
    public CompletableFuture<List<Account>> fetchAccountsAsync(Map<String, Object> params) {
        return exchange.fetchAccounts(params).thenApply(res -> toTypedList(res, Account::new));
    }

    public Balances fetchBalance(Map<String, Object> params) {
        Object res = exchange.fetchBalance(params).join();
        return new Balances(res);
    }
    public Balances fetchBalance() { return fetchBalance(null); }
    public CompletableFuture<Balances> fetchBalanceAsync(Map<String, Object> params) {
        return exchange.fetchBalance(params).thenApply(Balances::new);
    }

    public Balances fetchBalanceWs(Map<String, Object> params) {
        Object res = exchange.fetchBalanceWs(params).join();
        return new Balances(res);
    }
    public Balances fetchBalanceWs() { return fetchBalanceWs(null); }
    public CompletableFuture<Balances> fetchBalanceWsAsync(Map<String, Object> params) {
        return exchange.fetchBalanceWs(params).thenApply(Balances::new);
    }

    // ==========================================
    // WebSocket
    // ==========================================

    public List<Liquidation> watchLiquidations(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchLiquidations(symbol, since, limit, params).join();
        return toTypedList(res, Liquidation::new);
    }
    public List<Liquidation> watchLiquidations(String symbol) { return watchLiquidations(symbol, null, null, null); }

    public List<Liquidation> watchLiquidationsForSymbols(Object symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchLiquidationsForSymbols(symbols, since, limit, params).join();
        return toTypedList(res, Liquidation::new);
    }
    public List<Liquidation> watchLiquidationsForSymbols(Object symbols) { return watchLiquidationsForSymbols(symbols, null, null, null); }

    public List<Liquidation> watchMyLiquidations(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchMyLiquidations(symbol, since, limit, params).join();
        return toTypedList(res, Liquidation::new);
    }
    public List<Liquidation> watchMyLiquidations(String symbol) { return watchMyLiquidations(symbol, null, null, null); }

    public List<Liquidation> watchMyLiquidationsForSymbols(Object symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchMyLiquidationsForSymbols(symbols, since, limit, params).join();
        return toTypedList(res, Liquidation::new);
    }
    public List<Liquidation> watchMyLiquidationsForSymbols(Object symbols) { return watchMyLiquidationsForSymbols(symbols, null, null, null); }

    public List<Trade> watchTrades(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchTrades(symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> watchTrades(String symbol) { return watchTrades(symbol, null, null, null); }

    public List<Trade> watchTradesForSymbols(Object symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchTradesForSymbols(symbols, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> watchTradesForSymbols(Object symbols) { return watchTradesForSymbols(symbols, null, null, null); }

    public List<Trade> watchMyTradesForSymbols(Object symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchMyTradesForSymbols(symbols, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> watchMyTradesForSymbols(Object symbols) { return watchMyTradesForSymbols(symbols, null, null, null); }

    public List<Order> watchOrdersForSymbols(Object symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchOrdersForSymbols(symbols, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> watchOrdersForSymbols(Object symbols) { return watchOrdersForSymbols(symbols, null, null, null); }

    public OrderBook watchOrderBookForSymbols(Object symbols, Long limit, Map<String, Object> params) {
        Object res = exchange.watchOrderBookForSymbols(symbols, limit, params).join();
        return new OrderBook(res);
    }
    public OrderBook watchOrderBookForSymbols(Object symbols) { return watchOrderBookForSymbols(symbols, null, null); }

    public OrderBook watchOrderBook(String symbol, Long limit, Map<String, Object> params) {
        Object res = exchange.watchOrderBook(symbol, limit, params).join();
        return new OrderBook(res);
    }
    public OrderBook watchOrderBook(String symbol) { return watchOrderBook(symbol, null, null); }

    public FundingRate watchFundingRate(String symbol, Map<String, Object> params) {
        Object res = exchange.watchFundingRate(symbol, params).join();
        return new FundingRate(res);
    }
    public FundingRate watchFundingRate(String symbol) { return watchFundingRate(symbol, null); }

    public FundingRates watchFundingRates(Object symbols, Map<String, Object> params) {
        Object res = exchange.watchFundingRates(symbols, params).join();
        return new FundingRates(res);
    }
    public FundingRates watchFundingRates(Object symbols) { return watchFundingRates(symbols, null); }

    public List<OHLCV> watchOHLCV(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchOHLCV(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OHLCV::new);
    }
    public List<OHLCV> watchOHLCV(String symbol) { return watchOHLCV(symbol, "1m", null, null, null); }

    public Position watchPosition(String symbol, Map<String, Object> params) {
        Object res = exchange.watchPosition(symbol, params).join();
        return new Position(res);
    }
    public Position watchPosition() { return watchPosition(null, null); }

    public List<Position> watchPositions(List<String> symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchPositions(symbols, since, limit, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> watchPositions() { return watchPositions(null, null, null, null); }

    public List<Position> watchPositionForSymbols(List<String> symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchPositionForSymbols(symbols, since, limit, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> watchPositionForSymbols() { return watchPositionForSymbols(null, null, null, null); }

    public Balances watchBalance(Map<String, Object> params) {
        Object res = exchange.watchBalance(params).join();
        return new Balances(res);
    }
    public Balances watchBalance() { return watchBalance(null); }

    public Ticker watchTicker(String symbol, Map<String, Object> params) {
        Object res = exchange.watchTicker(symbol, params).join();
        return new Ticker(res);
    }
    public Ticker watchTicker(String symbol) { return watchTicker(symbol, null); }

    public Tickers watchBidsAsks(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.watchBidsAsks(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers watchBidsAsks() { return watchBidsAsks(null, null); }

    public Tickers watchTickers(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.watchTickers(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers watchTickers() { return watchTickers(null, null); }

    public List<Order> watchOrders(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchOrders(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> watchOrders() { return watchOrders(null, null, null, null); }

    public List<Trade> watchMyTrades(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.watchMyTrades(symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> watchMyTrades() { return watchMyTrades(null, null, null, null); }

    public Ticker watchMarkPrice(String symbol, Map<String, Object> params) {
        Object res = exchange.watchMarkPrice(symbol, params).join();
        return new Ticker(res);
    }
    public Ticker watchMarkPrice(String symbol) { return watchMarkPrice(symbol, null); }

    public Tickers watchMarkPrices(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.watchMarkPrices(symbols, params).join();
        return new Tickers(res);
    }
    public Tickers watchMarkPrices() { return watchMarkPrices(null, null); }

    // ==========================================
    // Funding
    // ==========================================

    public List<DepositAddress> fetchDepositAddresses(List<String> codes, Map<String, Object> params) {
        Object res = exchange.fetchDepositAddresses(codes, params).join();
        return toTypedList(res, DepositAddress::new);
    }
    public List<DepositAddress> fetchDepositAddresses() { return fetchDepositAddresses(null, null); }
    public CompletableFuture<List<DepositAddress>> fetchDepositAddressesAsync(List<String> codes, Map<String, Object> params) {
        return exchange.fetchDepositAddresses(codes, params).thenApply(res -> toTypedList(res, DepositAddress::new));
    }

    public TransferEntry transfer(String code, Double amount, String fromAccount, String toAccount, Map<String, Object> params) {
        Object res = exchange.transfer(code, amount, fromAccount, toAccount, params).join();
        return new TransferEntry(res);
    }
    public TransferEntry transfer(String code, Double amount, String fromAccount, String toAccount) { return transfer(code, amount, fromAccount, toAccount, null); }
    public CompletableFuture<TransferEntry> transferAsync(String code, Double amount, String fromAccount, String toAccount, Map<String, Object> params) {
        return exchange.transfer(code, amount, fromAccount, toAccount, params).thenApply(TransferEntry::new);
    }

    public Transaction withdraw(String code, Double amount, String address, String tag, Map<String, Object> params) {
        Object res = exchange.withdraw(code, amount, address, tag, params).join();
        return new Transaction(res);
    }
    public Transaction withdraw(String code, Double amount, String address) { return withdraw(code, amount, address, null, null); }
    public CompletableFuture<Transaction> withdrawAsync(String code, Double amount, String address, String tag, Map<String, Object> params) {
        return exchange.withdraw(code, amount, address, tag, params).thenApply(Transaction::new);
    }

    public List<DepositAddress> fetchDepositAddressesByNetwork(String code, Map<String, Object> params) {
        Object res = exchange.fetchDepositAddressesByNetwork(code, params).join();
        return toTypedList(res, DepositAddress::new);
    }
    public List<DepositAddress> fetchDepositAddressesByNetwork(String code) { return fetchDepositAddressesByNetwork(code, null); }
    public CompletableFuture<List<DepositAddress>> fetchDepositAddressesByNetworkAsync(String code, Map<String, Object> params) {
        return exchange.fetchDepositAddressesByNetwork(code, params).thenApply(res -> toTypedList(res, DepositAddress::new));
    }

    public DepositWithdrawFee fetchDepositWithdrawFee(String code, Map<String, Object> params) {
        Object res = exchange.fetchDepositWithdrawFee(code, params).join();
        return new DepositWithdrawFee(res);
    }
    public DepositWithdrawFee fetchDepositWithdrawFee(String code) { return fetchDepositWithdrawFee(code, null); }
    public CompletableFuture<DepositWithdrawFee> fetchDepositWithdrawFeeAsync(String code, Map<String, Object> params) {
        return exchange.fetchDepositWithdrawFee(code, params).thenApply(DepositWithdrawFee::new);
    }

    public List<Transaction> fetchDepositsWithdrawals(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchDepositsWithdrawals(code, since, limit, params).join();
        return toTypedList(res, Transaction::new);
    }
    public List<Transaction> fetchDepositsWithdrawals() { return fetchDepositsWithdrawals(null, null, null, null); }
    public CompletableFuture<List<Transaction>> fetchDepositsWithdrawalsAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchDepositsWithdrawals(code, since, limit, params).thenApply(res -> toTypedList(res, Transaction::new));
    }

    public List<Transaction> fetchDeposits(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchDeposits(code, since, limit, params).join();
        return toTypedList(res, Transaction::new);
    }
    public List<Transaction> fetchDeposits() { return fetchDeposits(null, null, null, null); }
    public CompletableFuture<List<Transaction>> fetchDepositsAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchDeposits(code, since, limit, params).thenApply(res -> toTypedList(res, Transaction::new));
    }

    public List<Transaction> fetchWithdrawals(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchWithdrawals(code, since, limit, params).join();
        return toTypedList(res, Transaction::new);
    }
    public List<Transaction> fetchWithdrawals() { return fetchWithdrawals(null, null, null, null); }
    public CompletableFuture<List<Transaction>> fetchWithdrawalsAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchWithdrawals(code, since, limit, params).thenApply(res -> toTypedList(res, Transaction::new));
    }

    public DepositAddress fetchDepositAddress(String code, Map<String, Object> params) {
        Object res = exchange.fetchDepositAddress(code, params).join();
        return new DepositAddress(res);
    }
    public DepositAddress fetchDepositAddress(String code) { return fetchDepositAddress(code, null); }
    public CompletableFuture<DepositAddress> fetchDepositAddressAsync(String code, Map<String, Object> params) {
        return exchange.fetchDepositAddress(code, params).thenApply(DepositAddress::new);
    }

    public List<Transaction> fetchTransactions(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchTransactions(code, since, limit, params).join();
        return toTypedList(res, Transaction::new);
    }
    public List<Transaction> fetchTransactions() { return fetchTransactions(null, null, null, null); }
    public CompletableFuture<List<Transaction>> fetchTransactionsAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchTransactions(code, since, limit, params).thenApply(res -> toTypedList(res, Transaction::new));
    }

    // ==========================================
    // Derivatives
    // ==========================================

    public MarginMode fetchMarginMode(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchMarginMode(symbol, params).join();
        return new MarginMode(res);
    }
    public MarginMode fetchMarginMode(String symbol) { return fetchMarginMode(symbol, null); }
    public CompletableFuture<MarginMode> fetchMarginModeAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchMarginMode(symbol, params).thenApply(MarginMode::new);
    }

    public MarginModes fetchMarginModes(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchMarginModes(symbols, params).join();
        return new MarginModes(res);
    }
    public MarginModes fetchMarginModes() { return fetchMarginModes(null, null); }
    public CompletableFuture<MarginModes> fetchMarginModesAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchMarginModes(symbols, params).thenApply(MarginModes::new);
    }

    public LeverageTiers fetchLeverageTiers(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchLeverageTiers(symbols, params).join();
        return new LeverageTiers(res);
    }
    public LeverageTiers fetchLeverageTiers() { return fetchLeverageTiers(null, null); }
    public CompletableFuture<LeverageTiers> fetchLeverageTiersAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchLeverageTiers(symbols, params).thenApply(LeverageTiers::new);
    }

    public FundingRates fetchFundingRates(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchFundingRates(symbols, params).join();
        return new FundingRates(res);
    }
    public FundingRates fetchFundingRates() { return fetchFundingRates(null, null); }
    public CompletableFuture<FundingRates> fetchFundingRatesAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchFundingRates(symbols, params).thenApply(FundingRates::new);
    }

    public FundingRates fetchFundingIntervals(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchFundingIntervals(symbols, params).join();
        return new FundingRates(res);
    }
    public FundingRates fetchFundingIntervals() { return fetchFundingIntervals(null, null); }
    public CompletableFuture<FundingRates> fetchFundingIntervalsAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchFundingIntervals(symbols, params).thenApply(FundingRates::new);
    }

    public Leverage fetchLeverage(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchLeverage(symbol, params).join();
        return new Leverage(res);
    }
    public Leverage fetchLeverage(String symbol) { return fetchLeverage(symbol, null); }
    public CompletableFuture<Leverage> fetchLeverageAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchLeverage(symbol, params).thenApply(Leverage::new);
    }

    public Leverages fetchLeverages(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchLeverages(symbols, params).join();
        return new Leverages(res);
    }
    public Leverages fetchLeverages() { return fetchLeverages(null, null); }
    public CompletableFuture<Leverages> fetchLeveragesAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchLeverages(symbols, params).thenApply(Leverages::new);
    }

    public MarginModification setMargin(String symbol, Double amount, Map<String, Object> params) {
        Object res = exchange.setMargin(symbol, amount, params).join();
        return new MarginModification(res);
    }
    public MarginModification setMargin(String symbol, Double amount) { return setMargin(symbol, amount, null); }
    public CompletableFuture<MarginModification> setMarginAsync(String symbol, Double amount, Map<String, Object> params) {
        return exchange.setMargin(symbol, amount, params).thenApply(MarginModification::new);
    }

    public LongShortRatio fetchLongShortRatio(String symbol, String timeframe, Map<String, Object> params) {
        Object res = exchange.fetchLongShortRatio(symbol, timeframe, params).join();
        return new LongShortRatio(res);
    }
    public LongShortRatio fetchLongShortRatio(String symbol) { return fetchLongShortRatio(symbol, null, null); }
    public CompletableFuture<LongShortRatio> fetchLongShortRatioAsync(String symbol, String timeframe, Map<String, Object> params) {
        return exchange.fetchLongShortRatio(symbol, timeframe, params).thenApply(LongShortRatio::new);
    }

    public List<LongShortRatio> fetchLongShortRatioHistory(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchLongShortRatioHistory(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, LongShortRatio::new);
    }
    public List<LongShortRatio> fetchLongShortRatioHistory() { return fetchLongShortRatioHistory(null, null, null, null, null); }
    public CompletableFuture<List<LongShortRatio>> fetchLongShortRatioHistoryAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchLongShortRatioHistory(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, LongShortRatio::new));
    }

    public List<MarginModification> fetchMarginAdjustmentHistory(String symbol, String type, Double since, Double limit, Map<String, Object> params) {
        Object res = exchange.fetchMarginAdjustmentHistory(symbol, type, since, limit, params).join();
        return toTypedList(res, MarginModification::new);
    }
    public List<MarginModification> fetchMarginAdjustmentHistory() { return fetchMarginAdjustmentHistory(null, null, null, null, null); }
    public CompletableFuture<List<MarginModification>> fetchMarginAdjustmentHistoryAsync(String symbol, String type, Double since, Double limit, Map<String, Object> params) {
        return exchange.fetchMarginAdjustmentHistory(symbol, type, since, limit, params).thenApply(res -> toTypedList(res, MarginModification::new));
    }

    public List<OpenInterest> fetchOpenInterestHistory(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOpenInterestHistory(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OpenInterest::new);
    }
    public List<OpenInterest> fetchOpenInterestHistory(String symbol) { return fetchOpenInterestHistory(symbol, "1h", null, null, null); }
    public CompletableFuture<List<OpenInterest>> fetchOpenInterestHistoryAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOpenInterestHistory(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, OpenInterest::new));
    }

    public OpenInterest fetchOpenInterest(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchOpenInterest(symbol, params).join();
        return new OpenInterest(res);
    }
    public OpenInterest fetchOpenInterest(String symbol) { return fetchOpenInterest(symbol, null); }
    public CompletableFuture<OpenInterest> fetchOpenInterestAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchOpenInterest(symbol, params).thenApply(OpenInterest::new);
    }

    public OpenInterests fetchOpenInterests(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchOpenInterests(symbols, params).join();
        return new OpenInterests(res);
    }
    public OpenInterests fetchOpenInterests() { return fetchOpenInterests(null, null); }
    public CompletableFuture<OpenInterests> fetchOpenInterestsAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchOpenInterests(symbols, params).thenApply(OpenInterests::new);
    }

    public Position fetchPosition(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchPosition(symbol, params).join();
        return new Position(res);
    }
    public Position fetchPosition(String symbol) { return fetchPosition(symbol, null); }
    public CompletableFuture<Position> fetchPositionAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchPosition(symbol, params).thenApply(Position::new);
    }

    public List<Position> fetchPositionWs(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchPositionWs(symbol, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionWs(String symbol) { return fetchPositionWs(symbol, null); }
    public CompletableFuture<List<Position>> fetchPositionWsAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchPositionWs(symbol, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<Position> fetchPositionsForSymbol(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchPositionsForSymbol(symbol, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionsForSymbol(String symbol) { return fetchPositionsForSymbol(symbol, null); }
    public CompletableFuture<List<Position>> fetchPositionsForSymbolAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchPositionsForSymbol(symbol, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<Position> fetchPositionsForSymbolWs(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchPositionsForSymbolWs(symbol, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionsForSymbolWs(String symbol) { return fetchPositionsForSymbolWs(symbol, null); }
    public CompletableFuture<List<Position>> fetchPositionsForSymbolWsAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchPositionsForSymbolWs(symbol, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<Position> fetchPositions(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchPositions(symbols, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositions() { return fetchPositions(null, null); }
    public CompletableFuture<List<Position>> fetchPositionsAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchPositions(symbols, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<Position> fetchPositionsWs(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchPositionsWs(symbols, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionsWs() { return fetchPositionsWs(null, null); }
    public CompletableFuture<List<Position>> fetchPositionsWsAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchPositionsWs(symbols, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<Position> fetchPositionsRisk(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchPositionsRisk(symbols, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionsRisk() { return fetchPositionsRisk(null, null); }
    public CompletableFuture<List<Position>> fetchPositionsRiskAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchPositionsRisk(symbols, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<FundingRateHistory> fetchFundingRateHistory(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchFundingRateHistory(symbol, since, limit, params).join();
        return toTypedList(res, FundingRateHistory::new);
    }
    public List<FundingRateHistory> fetchFundingRateHistory() { return fetchFundingRateHistory(null, null, null, null); }
    public CompletableFuture<List<FundingRateHistory>> fetchFundingRateHistoryAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchFundingRateHistory(symbol, since, limit, params).thenApply(res -> toTypedList(res, FundingRateHistory::new));
    }

    public List<FundingHistory> fetchFundingHistory(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchFundingHistory(symbol, since, limit, params).join();
        return toTypedList(res, FundingHistory::new);
    }
    public List<FundingHistory> fetchFundingHistory() { return fetchFundingHistory(null, null, null, null); }
    public CompletableFuture<List<FundingHistory>> fetchFundingHistoryAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchFundingHistory(symbol, since, limit, params).thenApply(res -> toTypedList(res, FundingHistory::new));
    }

    public FundingRate fetchFundingRate(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchFundingRate(symbol, params).join();
        return new FundingRate(res);
    }
    public FundingRate fetchFundingRate(String symbol) { return fetchFundingRate(symbol, null); }
    public CompletableFuture<FundingRate> fetchFundingRateAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchFundingRate(symbol, params).thenApply(FundingRate::new);
    }

    public FundingRate fetchFundingInterval(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchFundingInterval(symbol, params).join();
        return new FundingRate(res);
    }
    public FundingRate fetchFundingInterval(String symbol) { return fetchFundingInterval(symbol, null); }
    public CompletableFuture<FundingRate> fetchFundingIntervalAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchFundingInterval(symbol, params).thenApply(FundingRate::new);
    }

    public List<Position> fetchPositionHistory(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchPositionHistory(symbol, since, limit, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionHistory(String symbol) { return fetchPositionHistory(symbol, null, null, null); }
    public CompletableFuture<List<Position>> fetchPositionHistoryAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchPositionHistory(symbol, since, limit, params).thenApply(res -> toTypedList(res, Position::new));
    }

    public List<Position> fetchPositionsHistory(List<String> symbols, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchPositionsHistory(symbols, since, limit, params).join();
        return toTypedList(res, Position::new);
    }
    public List<Position> fetchPositionsHistory() { return fetchPositionsHistory(null, null, null, null); }
    public CompletableFuture<List<Position>> fetchPositionsHistoryAsync(List<String> symbols, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchPositionsHistory(symbols, since, limit, params).thenApply(res -> toTypedList(res, Position::new));
    }

    // ==========================================
    // Other
    // ==========================================

    public OrderBook fetchRestOrderBookSafe(Object symbol, Object limit, Map<String, Object> params) {
        Object res = exchange.fetchRestOrderBookSafe(symbol, limit, params).join();
        return new OrderBook(res);
    }
    public OrderBook fetchRestOrderBookSafe(Object symbol) { return fetchRestOrderBookSafe(symbol, null, null); }
    public CompletableFuture<OrderBook> fetchRestOrderBookSafeAsync(Object symbol, Object limit, Map<String, Object> params) {
        return exchange.fetchRestOrderBookSafe(symbol, limit, params).thenApply(OrderBook::new);
    }

    public Balance fetchPartialBalance(Object part, Map<String, Object> params) {
        Object res = exchange.fetchPartialBalance(part, params).join();
        return new Balance(res);
    }
    public Balance fetchPartialBalance(Object part) { return fetchPartialBalance(part, null); }
    public CompletableFuture<Balance> fetchPartialBalanceAsync(Object part, Map<String, Object> params) {
        return exchange.fetchPartialBalance(part, params).thenApply(Balance::new);
    }

    public Balance fetchFreeBalance(Map<String, Object> params) {
        Object res = exchange.fetchFreeBalance(params).join();
        return new Balance(res);
    }
    public Balance fetchFreeBalance() { return fetchFreeBalance(null); }
    public CompletableFuture<Balance> fetchFreeBalanceAsync(Map<String, Object> params) {
        return exchange.fetchFreeBalance(params).thenApply(Balance::new);
    }

    public Balance fetchUsedBalance(Map<String, Object> params) {
        Object res = exchange.fetchUsedBalance(params).join();
        return new Balance(res);
    }
    public Balance fetchUsedBalance() { return fetchUsedBalance(null); }
    public CompletableFuture<Balance> fetchUsedBalanceAsync(Map<String, Object> params) {
        return exchange.fetchUsedBalance(params).thenApply(Balance::new);
    }

    public Balance fetchTotalBalance(Map<String, Object> params) {
        Object res = exchange.fetchTotalBalance(params).join();
        return new Balance(res);
    }
    public Balance fetchTotalBalance() { return fetchTotalBalance(null); }
    public CompletableFuture<Balance> fetchTotalBalanceAsync(Map<String, Object> params) {
        return exchange.fetchTotalBalance(params).thenApply(Balance::new);
    }

    public Order fetchUnifiedOrder(Object order, Map<String, Object> params) {
        Object res = exchange.fetchUnifiedOrder(order, params).join();
        return new Order(res);
    }
    public Order fetchUnifiedOrder(Object order) { return fetchUnifiedOrder(order, null); }
    public CompletableFuture<Order> fetchUnifiedOrderAsync(Object order, Map<String, Object> params) {
        return exchange.fetchUnifiedOrder(order, params).thenApply(Order::new);
    }

    public Conversion fetchConvertTrade(String id, String code, Map<String, Object> params) {
        Object res = exchange.fetchConvertTrade(id, code, params).join();
        return new Conversion(res);
    }
    public Conversion fetchConvertTrade(String id) { return fetchConvertTrade(id, null, null); }
    public CompletableFuture<Conversion> fetchConvertTradeAsync(String id, String code, Map<String, Object> params) {
        return exchange.fetchConvertTrade(id, code, params).thenApply(Conversion::new);
    }

    public List<Conversion> fetchConvertTradeHistory(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchConvertTradeHistory(code, since, limit, params).join();
        return toTypedList(res, Conversion::new);
    }
    public List<Conversion> fetchConvertTradeHistory() { return fetchConvertTradeHistory(null, null, null, null); }
    public CompletableFuture<List<Conversion>> fetchConvertTradeHistoryAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchConvertTradeHistory(code, since, limit, params).thenApply(res -> toTypedList(res, Conversion::new));
    }

    public List<Liquidation> fetchLiquidations(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchLiquidations(symbol, since, limit, params).join();
        return toTypedList(res, Liquidation::new);
    }
    public List<Liquidation> fetchLiquidations(String symbol) { return fetchLiquidations(symbol, null, null, null); }
    public CompletableFuture<List<Liquidation>> fetchLiquidationsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchLiquidations(symbol, since, limit, params).thenApply(res -> toTypedList(res, Liquidation::new));
    }

    public Greeks fetchGreeks(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchGreeks(symbol, params).join();
        return new Greeks(res);
    }
    public Greeks fetchGreeks(String symbol) { return fetchGreeks(symbol, null); }
    public CompletableFuture<Greeks> fetchGreeksAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchGreeks(symbol, params).thenApply(Greeks::new);
    }

    public List<Greeks> fetchAllGreeks(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchAllGreeks(symbols, params).join();
        return toTypedList(res, Greeks::new);
    }
    public List<Greeks> fetchAllGreeks() { return fetchAllGreeks(null, null); }
    public CompletableFuture<List<Greeks>> fetchAllGreeksAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchAllGreeks(symbols, params).thenApply(res -> toTypedList(res, Greeks::new));
    }

    public OptionChain fetchOptionChain(String code, Map<String, Object> params) {
        Object res = exchange.fetchOptionChain(code, params).join();
        return new OptionChain(res);
    }
    public OptionChain fetchOptionChain(String code) { return fetchOptionChain(code, null); }
    public CompletableFuture<OptionChain> fetchOptionChainAsync(String code, Map<String, Object> params) {
        return exchange.fetchOptionChain(code, params).thenApply(OptionChain::new);
    }

    public Option fetchOption(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchOption(symbol, params).join();
        return new Option(res);
    }
    public Option fetchOption(String symbol) { return fetchOption(symbol, null); }
    public CompletableFuture<Option> fetchOptionAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchOption(symbol, params).thenApply(Option::new);
    }

    public Conversion fetchConvertQuote(String fromCode, String toCode, Double amount, Map<String, Object> params) {
        Object res = exchange.fetchConvertQuote(fromCode, toCode, amount, params).join();
        return new Conversion(res);
    }
    public Conversion fetchConvertQuote(String fromCode, String toCode) { return fetchConvertQuote(fromCode, toCode, null, null); }
    public CompletableFuture<Conversion> fetchConvertQuoteAsync(String fromCode, String toCode, Double amount, Map<String, Object> params) {
        return exchange.fetchConvertQuote(fromCode, toCode, amount, params).thenApply(Conversion::new);
    }

    public OrderBook fetchL3OrderBook(String symbol, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchL3OrderBook(symbol, limit, params).join();
        return new OrderBook(res);
    }
    public OrderBook fetchL3OrderBook(String symbol) { return fetchL3OrderBook(symbol, null, null); }
    public CompletableFuture<OrderBook> fetchL3OrderBookAsync(String symbol, Long limit, Map<String, Object> params) {
        return exchange.fetchL3OrderBook(symbol, limit, params).thenApply(OrderBook::new);
    }

    public LastPrices fetchLastPrices(List<String> symbols, Map<String, Object> params) {
        Object res = exchange.fetchLastPrices(symbols, params).join();
        return new LastPrices(res);
    }
    public LastPrices fetchLastPrices() { return fetchLastPrices(null, null); }
    public CompletableFuture<LastPrices> fetchLastPricesAsync(List<String> symbols, Map<String, Object> params) {
        return exchange.fetchLastPrices(symbols, params).thenApply(LastPrices::new);
    }

    public Currencies fetchConvertCurrencies(Map<String, Object> params) {
        Object res = exchange.fetchConvertCurrencies(params).join();
        return new Currencies(res);
    }
    public Currencies fetchConvertCurrencies() { return fetchConvertCurrencies(null); }
    public CompletableFuture<Currencies> fetchConvertCurrenciesAsync(Map<String, Object> params) {
        return exchange.fetchConvertCurrencies(params).thenApply(Currencies::new);
    }

    public List<OHLCV> fetchMarkOHLCV(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchMarkOHLCV(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OHLCV::new);
    }
    public List<OHLCV> fetchMarkOHLCV(String symbol) { return fetchMarkOHLCV(symbol, "1m", null, null, null); }
    public CompletableFuture<List<OHLCV>> fetchMarkOHLCVAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchMarkOHLCV(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, OHLCV::new));
    }

    public List<OHLCV> fetchIndexOHLCV(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchIndexOHLCV(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OHLCV::new);
    }
    public List<OHLCV> fetchIndexOHLCV(String symbol) { return fetchIndexOHLCV(symbol, "1m", null, null, null); }
    public CompletableFuture<List<OHLCV>> fetchIndexOHLCVAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchIndexOHLCV(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, OHLCV::new));
    }

    public List<OHLCV> fetchPremiumIndexOHLCV(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchPremiumIndexOHLCV(symbol, timeframe, since, limit, params).join();
        return toTypedList(res, OHLCV::new);
    }
    public List<OHLCV> fetchPremiumIndexOHLCV(String symbol) { return fetchPremiumIndexOHLCV(symbol, "1m", null, null, null); }
    public CompletableFuture<List<OHLCV>> fetchPremiumIndexOHLCVAsync(String symbol, String timeframe, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchPremiumIndexOHLCV(symbol, timeframe, since, limit, params).thenApply(res -> toTypedList(res, OHLCV::new));
    }

    public TransferEntry fetchTransfer(String id, String code, Map<String, Object> params) {
        Object res = exchange.fetchTransfer(id, code, params).join();
        return new TransferEntry(res);
    }
    public TransferEntry fetchTransfer(String id) { return fetchTransfer(id, null, null); }
    public CompletableFuture<TransferEntry> fetchTransferAsync(String id, String code, Map<String, Object> params) {
        return exchange.fetchTransfer(id, code, params).thenApply(TransferEntry::new);
    }

    public List<TransferEntry> fetchTransfers(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchTransfers(code, since, limit, params).join();
        return toTypedList(res, TransferEntry::new);
    }
    public List<TransferEntry> fetchTransfers() { return fetchTransfers(null, null, null, null); }
    public CompletableFuture<List<TransferEntry>> fetchTransfersAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchTransfers(code, since, limit, params).thenApply(res -> toTypedList(res, TransferEntry::new));
    }

    // ==========================================
    // Borrow
    // ==========================================

    public CrossBorrowRates fetchCrossBorrowRates(Map<String, Object> params) {
        Object res = exchange.fetchCrossBorrowRates(params).join();
        return new CrossBorrowRates(res);
    }
    public CrossBorrowRates fetchCrossBorrowRates() { return fetchCrossBorrowRates(null); }
    public CompletableFuture<CrossBorrowRates> fetchCrossBorrowRatesAsync(Map<String, Object> params) {
        return exchange.fetchCrossBorrowRates(params).thenApply(CrossBorrowRates::new);
    }

    public IsolatedBorrowRates fetchIsolatedBorrowRates(Map<String, Object> params) {
        Object res = exchange.fetchIsolatedBorrowRates(params).join();
        return new IsolatedBorrowRates(res);
    }
    public IsolatedBorrowRates fetchIsolatedBorrowRates() { return fetchIsolatedBorrowRates(null); }
    public CompletableFuture<IsolatedBorrowRates> fetchIsolatedBorrowRatesAsync(Map<String, Object> params) {
        return exchange.fetchIsolatedBorrowRates(params).thenApply(IsolatedBorrowRates::new);
    }

    public List<BorrowInterest> fetchBorrowInterest(String code, String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchBorrowInterest(code, symbol, since, limit, params).join();
        return toTypedList(res, BorrowInterest::new);
    }
    public List<BorrowInterest> fetchBorrowInterest() { return fetchBorrowInterest(null, null, null, null, null); }
    public CompletableFuture<List<BorrowInterest>> fetchBorrowInterestAsync(String code, String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchBorrowInterest(code, symbol, since, limit, params).thenApply(res -> toTypedList(res, BorrowInterest::new));
    }

    public CrossBorrowRate fetchCrossBorrowRate(String code, Map<String, Object> params) {
        Object res = exchange.fetchCrossBorrowRate(code, params).join();
        return new CrossBorrowRate(res);
    }
    public CrossBorrowRate fetchCrossBorrowRate(String code) { return fetchCrossBorrowRate(code, null); }
    public CompletableFuture<CrossBorrowRate> fetchCrossBorrowRateAsync(String code, Map<String, Object> params) {
        return exchange.fetchCrossBorrowRate(code, params).thenApply(CrossBorrowRate::new);
    }

    public IsolatedBorrowRate fetchIsolatedBorrowRate(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchIsolatedBorrowRate(symbol, params).join();
        return new IsolatedBorrowRate(res);
    }
    public IsolatedBorrowRate fetchIsolatedBorrowRate(String symbol) { return fetchIsolatedBorrowRate(symbol, null); }
    public CompletableFuture<IsolatedBorrowRate> fetchIsolatedBorrowRateAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchIsolatedBorrowRate(symbol, params).thenApply(IsolatedBorrowRate::new);
    }

    // ==========================================
    // Trading
    // ==========================================

    public DepositAddress createDepositAddress(String code, Map<String, Object> params) {
        Object res = exchange.createDepositAddress(code, params).join();
        return new DepositAddress(res);
    }
    public DepositAddress createDepositAddress(String code) { return createDepositAddress(code, null); }
    public CompletableFuture<DepositAddress> createDepositAddressAsync(String code, Map<String, Object> params) {
        return exchange.createDepositAddress(code, params).thenApply(DepositAddress::new);
    }

    public Order editLimitBuyOrder(String id, String symbol, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.editLimitBuyOrder(id, symbol, amount, price, params).join();
        return new Order(res);
    }
    public Order editLimitBuyOrder(String id, String symbol, Double amount) { return editLimitBuyOrder(id, symbol, amount, null, null); }
    public CompletableFuture<Order> editLimitBuyOrderAsync(String id, String symbol, Double amount, Double price, Map<String, Object> params) {
        return exchange.editLimitBuyOrder(id, symbol, amount, price, params).thenApply(Order::new);
    }

    public Order editLimitSellOrder(String id, String symbol, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.editLimitSellOrder(id, symbol, amount, price, params).join();
        return new Order(res);
    }
    public Order editLimitSellOrder(String id, String symbol, Double amount) { return editLimitSellOrder(id, symbol, amount, null, null); }
    public CompletableFuture<Order> editLimitSellOrderAsync(String id, String symbol, Double amount, Double price, Map<String, Object> params) {
        return exchange.editLimitSellOrder(id, symbol, amount, price, params).thenApply(Order::new);
    }

    public Order editLimitOrder(String id, String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.editLimitOrder(id, symbol, side, amount, price, params).join();
        return new Order(res);
    }
    public Order editLimitOrder(String id, String symbol, String side, Double amount) { return editLimitOrder(id, symbol, side, amount, null, null); }
    public CompletableFuture<Order> editLimitOrderAsync(String id, String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.editLimitOrder(id, symbol, side, amount, price, params).thenApply(Order::new);
    }

    public Order editOrder(String id, String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.editOrder(id, symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order editOrder(String id, String symbol, String type, String side) { return editOrder(id, symbol, type, side, null, null, null); }
    public CompletableFuture<Order> editOrderAsync(String id, String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.editOrder(id, symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order editOrderWithClientOrderId(String clientOrderId, String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.editOrderWithClientOrderId(clientOrderId, symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order editOrderWithClientOrderId(String clientOrderId, String symbol, String type, String side) { return editOrderWithClientOrderId(clientOrderId, symbol, type, side, null, null, null); }
    public CompletableFuture<Order> editOrderWithClientOrderIdAsync(String clientOrderId, String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.editOrderWithClientOrderId(clientOrderId, symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order editOrderWs(String id, String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.editOrderWs(id, symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order editOrderWs(String id, String symbol, String type, String side) { return editOrderWs(id, symbol, type, side, null, null, null); }
    public CompletableFuture<Order> editOrderWsAsync(String id, String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.editOrderWs(id, symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order fetchOrder(String id, String symbol, Map<String, Object> params) {
        Object res = exchange.fetchOrder(id, symbol, params).join();
        return new Order(res);
    }
    public Order fetchOrder(String id) { return fetchOrder(id, null, null); }
    public CompletableFuture<Order> fetchOrderAsync(String id, String symbol, Map<String, Object> params) {
        return exchange.fetchOrder(id, symbol, params).thenApply(Order::new);
    }

    public Order fetchOrderWithClientOrderId(String clientOrderId, String symbol, Map<String, Object> params) {
        Object res = exchange.fetchOrderWithClientOrderId(clientOrderId, symbol, params).join();
        return new Order(res);
    }
    public Order fetchOrderWithClientOrderId(String clientOrderId) { return fetchOrderWithClientOrderId(clientOrderId, null, null); }
    public CompletableFuture<Order> fetchOrderWithClientOrderIdAsync(String clientOrderId, String symbol, Map<String, Object> params) {
        return exchange.fetchOrderWithClientOrderId(clientOrderId, symbol, params).thenApply(Order::new);
    }

    public Order fetchOrderWs(String id, String symbol, Map<String, Object> params) {
        Object res = exchange.fetchOrderWs(id, symbol, params).join();
        return new Order(res);
    }
    public Order fetchOrderWs(String id) { return fetchOrderWs(id, null, null); }
    public CompletableFuture<Order> fetchOrderWsAsync(String id, String symbol, Map<String, Object> params) {
        return exchange.fetchOrderWs(id, symbol, params).thenApply(Order::new);
    }

    public String fetchOrderStatus(String id, String symbol, Map<String, Object> params) {
        Object res = exchange.fetchOrderStatus(id, symbol, params).join();
        return (String) res;
    }
    public String fetchOrderStatus(String id) { return fetchOrderStatus(id, null, null); }
    public CompletableFuture<String> fetchOrderStatusAsync(String id, String symbol, Map<String, Object> params) {
        return exchange.fetchOrderStatus(id, symbol, params).thenApply(res -> (String) res);
    }

    public Order createOrder(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createOrder(symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createOrder(String symbol, String type, String side, Double amount) { return createOrder(symbol, type, side, amount, null, null); }
    public CompletableFuture<Order> createOrderAsync(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createOrder(symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Conversion createConvertTrade(String id, String fromCode, String toCode, Double amount, Map<String, Object> params) {
        Object res = exchange.createConvertTrade(id, fromCode, toCode, amount, params).join();
        return new Conversion(res);
    }
    public Conversion createConvertTrade(String id, String fromCode, String toCode) { return createConvertTrade(id, fromCode, toCode, null, null); }
    public CompletableFuture<Conversion> createConvertTradeAsync(String id, String fromCode, String toCode, Double amount, Map<String, Object> params) {
        return exchange.createConvertTrade(id, fromCode, toCode, amount, params).thenApply(Conversion::new);
    }

    public Order createTrailingAmountOrder(String symbol, String type, String side, Double amount, Double price, Double trailingAmount, Double trailingTriggerPrice, Map<String, Object> params) {
        Object res = exchange.createTrailingAmountOrder(symbol, type, side, amount, price, trailingAmount, trailingTriggerPrice, params).join();
        return new Order(res);
    }
    public Order createTrailingAmountOrder(String symbol, String type, String side, Double amount) { return createTrailingAmountOrder(symbol, type, side, amount, null, null, null, null); }
    public CompletableFuture<Order> createTrailingAmountOrderAsync(String symbol, String type, String side, Double amount, Double price, Double trailingAmount, Double trailingTriggerPrice, Map<String, Object> params) {
        return exchange.createTrailingAmountOrder(symbol, type, side, amount, price, trailingAmount, trailingTriggerPrice, params).thenApply(Order::new);
    }

    public Order createTrailingAmountOrderWs(String symbol, String type, String side, Double amount, Double price, Double trailingAmount, Double trailingTriggerPrice, Map<String, Object> params) {
        Object res = exchange.createTrailingAmountOrderWs(symbol, type, side, amount, price, trailingAmount, trailingTriggerPrice, params).join();
        return new Order(res);
    }
    public Order createTrailingAmountOrderWs(String symbol, String type, String side, Double amount) { return createTrailingAmountOrderWs(symbol, type, side, amount, null, null, null, null); }
    public CompletableFuture<Order> createTrailingAmountOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Double trailingAmount, Double trailingTriggerPrice, Map<String, Object> params) {
        return exchange.createTrailingAmountOrderWs(symbol, type, side, amount, price, trailingAmount, trailingTriggerPrice, params).thenApply(Order::new);
    }

    public Order createTrailingPercentOrder(String symbol, String type, String side, Double amount, Double price, Double trailingPercent, Double trailingTriggerPrice, Map<String, Object> params) {
        Object res = exchange.createTrailingPercentOrder(symbol, type, side, amount, price, trailingPercent, trailingTriggerPrice, params).join();
        return new Order(res);
    }
    public Order createTrailingPercentOrder(String symbol, String type, String side, Double amount) { return createTrailingPercentOrder(symbol, type, side, amount, null, null, null, null); }
    public CompletableFuture<Order> createTrailingPercentOrderAsync(String symbol, String type, String side, Double amount, Double price, Double trailingPercent, Double trailingTriggerPrice, Map<String, Object> params) {
        return exchange.createTrailingPercentOrder(symbol, type, side, amount, price, trailingPercent, trailingTriggerPrice, params).thenApply(Order::new);
    }

    public Order createTrailingPercentOrderWs(String symbol, String type, String side, Double amount, Double price, Double trailingPercent, Double trailingTriggerPrice, Map<String, Object> params) {
        Object res = exchange.createTrailingPercentOrderWs(symbol, type, side, amount, price, trailingPercent, trailingTriggerPrice, params).join();
        return new Order(res);
    }
    public Order createTrailingPercentOrderWs(String symbol, String type, String side, Double amount) { return createTrailingPercentOrderWs(symbol, type, side, amount, null, null, null, null); }
    public CompletableFuture<Order> createTrailingPercentOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Double trailingPercent, Double trailingTriggerPrice, Map<String, Object> params) {
        return exchange.createTrailingPercentOrderWs(symbol, type, side, amount, price, trailingPercent, trailingTriggerPrice, params).thenApply(Order::new);
    }

    public Order createMarketOrderWithCost(String symbol, String side, Double cost, Map<String, Object> params) {
        Object res = exchange.createMarketOrderWithCost(symbol, side, cost, params).join();
        return new Order(res);
    }
    public Order createMarketOrderWithCost(String symbol, String side, Double cost) { return createMarketOrderWithCost(symbol, side, cost, null); }
    public CompletableFuture<Order> createMarketOrderWithCostAsync(String symbol, String side, Double cost, Map<String, Object> params) {
        return exchange.createMarketOrderWithCost(symbol, side, cost, params).thenApply(Order::new);
    }

    public Order createMarketBuyOrderWithCost(String symbol, Double cost, Map<String, Object> params) {
        Object res = exchange.createMarketBuyOrderWithCost(symbol, cost, params).join();
        return new Order(res);
    }
    public Order createMarketBuyOrderWithCost(String symbol, Double cost) { return createMarketBuyOrderWithCost(symbol, cost, null); }
    public CompletableFuture<Order> createMarketBuyOrderWithCostAsync(String symbol, Double cost, Map<String, Object> params) {
        return exchange.createMarketBuyOrderWithCost(symbol, cost, params).thenApply(Order::new);
    }

    public Order createMarketSellOrderWithCost(String symbol, Double cost, Map<String, Object> params) {
        Object res = exchange.createMarketSellOrderWithCost(symbol, cost, params).join();
        return new Order(res);
    }
    public Order createMarketSellOrderWithCost(String symbol, Double cost) { return createMarketSellOrderWithCost(symbol, cost, null); }
    public CompletableFuture<Order> createMarketSellOrderWithCostAsync(String symbol, Double cost, Map<String, Object> params) {
        return exchange.createMarketSellOrderWithCost(symbol, cost, params).thenApply(Order::new);
    }

    public Order createMarketOrderWithCostWs(String symbol, String side, Double cost, Map<String, Object> params) {
        Object res = exchange.createMarketOrderWithCostWs(symbol, side, cost, params).join();
        return new Order(res);
    }
    public Order createMarketOrderWithCostWs(String symbol, String side, Double cost) { return createMarketOrderWithCostWs(symbol, side, cost, null); }
    public CompletableFuture<Order> createMarketOrderWithCostWsAsync(String symbol, String side, Double cost, Map<String, Object> params) {
        return exchange.createMarketOrderWithCostWs(symbol, side, cost, params).thenApply(Order::new);
    }

    public Order createTriggerOrder(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createTriggerOrder(symbol, type, side, amount, price, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createTriggerOrder(String symbol, String type, String side, Double amount) { return createTriggerOrder(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createTriggerOrderAsync(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        return exchange.createTriggerOrder(symbol, type, side, amount, price, triggerPrice, params).thenApply(Order::new);
    }

    public Order createTriggerOrderWs(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createTriggerOrderWs(symbol, type, side, amount, price, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createTriggerOrderWs(String symbol, String type, String side, Double amount) { return createTriggerOrderWs(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createTriggerOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        return exchange.createTriggerOrderWs(symbol, type, side, amount, price, triggerPrice, params).thenApply(Order::new);
    }

    public Order createStopLossOrder(String symbol, String type, String side, Double amount, Double price, Double stopLossPrice, Map<String, Object> params) {
        Object res = exchange.createStopLossOrder(symbol, type, side, amount, price, stopLossPrice, params).join();
        return new Order(res);
    }
    public Order createStopLossOrder(String symbol, String type, String side, Double amount) { return createStopLossOrder(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createStopLossOrderAsync(String symbol, String type, String side, Double amount, Double price, Double stopLossPrice, Map<String, Object> params) {
        return exchange.createStopLossOrder(symbol, type, side, amount, price, stopLossPrice, params).thenApply(Order::new);
    }

    public Order createStopLossOrderWs(String symbol, String type, String side, Double amount, Double price, Double stopLossPrice, Map<String, Object> params) {
        Object res = exchange.createStopLossOrderWs(symbol, type, side, amount, price, stopLossPrice, params).join();
        return new Order(res);
    }
    public Order createStopLossOrderWs(String symbol, String type, String side, Double amount) { return createStopLossOrderWs(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createStopLossOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Double stopLossPrice, Map<String, Object> params) {
        return exchange.createStopLossOrderWs(symbol, type, side, amount, price, stopLossPrice, params).thenApply(Order::new);
    }

    public Order createTakeProfitOrder(String symbol, String type, String side, Double amount, Double price, Double takeProfitPrice, Map<String, Object> params) {
        Object res = exchange.createTakeProfitOrder(symbol, type, side, amount, price, takeProfitPrice, params).join();
        return new Order(res);
    }
    public Order createTakeProfitOrder(String symbol, String type, String side, Double amount) { return createTakeProfitOrder(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createTakeProfitOrderAsync(String symbol, String type, String side, Double amount, Double price, Double takeProfitPrice, Map<String, Object> params) {
        return exchange.createTakeProfitOrder(symbol, type, side, amount, price, takeProfitPrice, params).thenApply(Order::new);
    }

    public Order createTakeProfitOrderWs(String symbol, String type, String side, Double amount, Double price, Double takeProfitPrice, Map<String, Object> params) {
        Object res = exchange.createTakeProfitOrderWs(symbol, type, side, amount, price, takeProfitPrice, params).join();
        return new Order(res);
    }
    public Order createTakeProfitOrderWs(String symbol, String type, String side, Double amount) { return createTakeProfitOrderWs(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createTakeProfitOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Double takeProfitPrice, Map<String, Object> params) {
        return exchange.createTakeProfitOrderWs(symbol, type, side, amount, price, takeProfitPrice, params).thenApply(Order::new);
    }

    public Order createOrderWithTakeProfitAndStopLoss(String symbol, String type, String side, Double amount, Double price, Double takeProfit, Double stopLoss, Map<String, Object> params) {
        Object res = exchange.createOrderWithTakeProfitAndStopLoss(symbol, type, side, amount, price, takeProfit, stopLoss, params).join();
        return new Order(res);
    }
    public Order createOrderWithTakeProfitAndStopLoss(String symbol, String type, String side, Double amount) { return createOrderWithTakeProfitAndStopLoss(symbol, type, side, amount, null, null, null, null); }
    public CompletableFuture<Order> createOrderWithTakeProfitAndStopLossAsync(String symbol, String type, String side, Double amount, Double price, Double takeProfit, Double stopLoss, Map<String, Object> params) {
        return exchange.createOrderWithTakeProfitAndStopLoss(symbol, type, side, amount, price, takeProfit, stopLoss, params).thenApply(Order::new);
    }

    public Order createOrderWithTakeProfitAndStopLossWs(String symbol, String type, String side, Double amount, Double price, Double takeProfit, Double stopLoss, Map<String, Object> params) {
        Object res = exchange.createOrderWithTakeProfitAndStopLossWs(symbol, type, side, amount, price, takeProfit, stopLoss, params).join();
        return new Order(res);
    }
    public Order createOrderWithTakeProfitAndStopLossWs(String symbol, String type, String side, Double amount) { return createOrderWithTakeProfitAndStopLossWs(symbol, type, side, amount, null, null, null, null); }
    public CompletableFuture<Order> createOrderWithTakeProfitAndStopLossWsAsync(String symbol, String type, String side, Double amount, Double price, Double takeProfit, Double stopLoss, Map<String, Object> params) {
        return exchange.createOrderWithTakeProfitAndStopLossWs(symbol, type, side, amount, price, takeProfit, stopLoss, params).thenApply(Order::new);
    }

    public List<Order> createOrders(Object orders, Map<String, Object> params) {
        Object res = exchange.createOrders(orders, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> createOrders(Object orders) { return createOrders(orders, null); }
    public CompletableFuture<List<Order>> createOrdersAsync(Object orders, Map<String, Object> params) {
        return exchange.createOrders(orders, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> editOrders(Object orders, Map<String, Object> params) {
        Object res = exchange.editOrders(orders, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> editOrders(Object orders) { return editOrders(orders, null); }
    public CompletableFuture<List<Order>> editOrdersAsync(Object orders, Map<String, Object> params) {
        return exchange.editOrders(orders, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public Order createOrderWs(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createOrderWs(symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createOrderWs(String symbol, String type, String side, Double amount) { return createOrderWs(symbol, type, side, amount, null, null); }
    public CompletableFuture<Order> createOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createOrderWs(symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order cancelOrder(String id, String symbol, Map<String, Object> params) {
        Object res = exchange.cancelOrder(id, symbol, params).join();
        return new Order(res);
    }
    public Order cancelOrder(String id) { return cancelOrder(id, null, null); }
    public CompletableFuture<Order> cancelOrderAsync(String id, String symbol, Map<String, Object> params) {
        return exchange.cancelOrder(id, symbol, params).thenApply(Order::new);
    }

    public Order cancelOrderWithClientOrderId(String clientOrderId, String symbol, Map<String, Object> params) {
        Object res = exchange.cancelOrderWithClientOrderId(clientOrderId, symbol, params).join();
        return new Order(res);
    }
    public Order cancelOrderWithClientOrderId(String clientOrderId) { return cancelOrderWithClientOrderId(clientOrderId, null, null); }
    public CompletableFuture<Order> cancelOrderWithClientOrderIdAsync(String clientOrderId, String symbol, Map<String, Object> params) {
        return exchange.cancelOrderWithClientOrderId(clientOrderId, symbol, params).thenApply(Order::new);
    }

    public Order cancelOrderWs(String id, String symbol, Map<String, Object> params) {
        Object res = exchange.cancelOrderWs(id, symbol, params).join();
        return new Order(res);
    }
    public Order cancelOrderWs(String id) { return cancelOrderWs(id, null, null); }
    public CompletableFuture<Order> cancelOrderWsAsync(String id, String symbol, Map<String, Object> params) {
        return exchange.cancelOrderWs(id, symbol, params).thenApply(Order::new);
    }

    public List<Order> cancelOrders(Object ids, String symbol, Map<String, Object> params) {
        Object res = exchange.cancelOrders(ids, symbol, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> cancelOrders(Object ids) { return cancelOrders(ids, null, null); }
    public CompletableFuture<List<Order>> cancelOrdersAsync(Object ids, String symbol, Map<String, Object> params) {
        return exchange.cancelOrders(ids, symbol, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> cancelOrdersWithClientOrderIds(Object clientOrderIds, String symbol, Map<String, Object> params) {
        Object res = exchange.cancelOrdersWithClientOrderIds(clientOrderIds, symbol, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> cancelOrdersWithClientOrderIds(Object clientOrderIds) { return cancelOrdersWithClientOrderIds(clientOrderIds, null, null); }
    public CompletableFuture<List<Order>> cancelOrdersWithClientOrderIdsAsync(Object clientOrderIds, String symbol, Map<String, Object> params) {
        return exchange.cancelOrdersWithClientOrderIds(clientOrderIds, symbol, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> cancelOrdersWs(Object ids, String symbol, Map<String, Object> params) {
        Object res = exchange.cancelOrdersWs(ids, symbol, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> cancelOrdersWs(Object ids) { return cancelOrdersWs(ids, null, null); }
    public CompletableFuture<List<Order>> cancelOrdersWsAsync(Object ids, String symbol, Map<String, Object> params) {
        return exchange.cancelOrdersWs(ids, symbol, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> cancelAllOrders(String symbol, Map<String, Object> params) {
        Object res = exchange.cancelAllOrders(symbol, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> cancelAllOrders() { return cancelAllOrders(null, null); }
    public CompletableFuture<List<Order>> cancelAllOrdersAsync(String symbol, Map<String, Object> params) {
        return exchange.cancelAllOrders(symbol, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> cancelOrdersForSymbols(Object orders, Map<String, Object> params) {
        Object res = exchange.cancelOrdersForSymbols(orders, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> cancelOrdersForSymbols(Object orders) { return cancelOrdersForSymbols(orders, null); }
    public CompletableFuture<List<Order>> cancelOrdersForSymbolsAsync(Object orders, Map<String, Object> params) {
        return exchange.cancelOrdersForSymbols(orders, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> cancelAllOrdersWs(String symbol, Map<String, Object> params) {
        Object res = exchange.cancelAllOrdersWs(symbol, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> cancelAllOrdersWs() { return cancelAllOrdersWs(null, null); }
    public CompletableFuture<List<Order>> cancelAllOrdersWsAsync(String symbol, Map<String, Object> params) {
        return exchange.cancelAllOrdersWs(symbol, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchOrders(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOrders(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchOrders() { return fetchOrders(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchOrdersAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOrders(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchOrdersWs(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOrdersWs(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchOrdersWs() { return fetchOrdersWs(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchOrdersWsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOrdersWs(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Trade> fetchOrderTrades(String id, String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOrderTrades(id, symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> fetchOrderTrades(String id) { return fetchOrderTrades(id, null, null, null, null); }
    public CompletableFuture<List<Trade>> fetchOrderTradesAsync(String id, String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOrderTrades(id, symbol, since, limit, params).thenApply(res -> toTypedList(res, Trade::new));
    }

    public List<Order> fetchOpenOrders(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOpenOrders(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchOpenOrders() { return fetchOpenOrders(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchOpenOrdersAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOpenOrders(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchOpenOrdersWs(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOpenOrdersWs(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchOpenOrdersWs() { return fetchOpenOrdersWs(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchOpenOrdersWsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOpenOrdersWs(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchClosedOrders(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchClosedOrders(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchClosedOrders() { return fetchClosedOrders(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchClosedOrdersAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchClosedOrders(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchCanceledAndClosedOrders(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchCanceledAndClosedOrders(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchCanceledAndClosedOrders() { return fetchCanceledAndClosedOrders(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchCanceledAndClosedOrdersAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchCanceledAndClosedOrders(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchClosedOrdersWs(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchClosedOrdersWs(symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchClosedOrdersWs() { return fetchClosedOrdersWs(null, null, null, null); }
    public CompletableFuture<List<Order>> fetchClosedOrdersWsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchClosedOrdersWs(symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Trade> fetchMyTrades(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchMyTrades(symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> fetchMyTrades() { return fetchMyTrades(null, null, null, null); }
    public CompletableFuture<List<Trade>> fetchMyTradesAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchMyTrades(symbol, since, limit, params).thenApply(res -> toTypedList(res, Trade::new));
    }

    public List<Liquidation> fetchMyLiquidations(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchMyLiquidations(symbol, since, limit, params).join();
        return toTypedList(res, Liquidation::new);
    }
    public List<Liquidation> fetchMyLiquidations() { return fetchMyLiquidations(null, null, null, null); }
    public CompletableFuture<List<Liquidation>> fetchMyLiquidationsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchMyLiquidations(symbol, since, limit, params).thenApply(res -> toTypedList(res, Liquidation::new));
    }

    public List<Trade> fetchMyTradesWs(String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchMyTradesWs(symbol, since, limit, params).join();
        return toTypedList(res, Trade::new);
    }
    public List<Trade> fetchMyTradesWs() { return fetchMyTradesWs(null, null, null, null); }
    public CompletableFuture<List<Trade>> fetchMyTradesWsAsync(String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchMyTradesWs(symbol, since, limit, params).thenApply(res -> toTypedList(res, Trade::new));
    }

    public Order createLimitOrder(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createLimitOrder(symbol, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createLimitOrder(String symbol, String side, Double amount, Double price) { return createLimitOrder(symbol, side, amount, price, null); }
    public CompletableFuture<Order> createLimitOrderAsync(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createLimitOrder(symbol, side, amount, price, params).thenApply(Order::new);
    }

    public Order createLimitOrderWs(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createLimitOrderWs(symbol, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createLimitOrderWs(String symbol, String side, Double amount, Double price) { return createLimitOrderWs(symbol, side, amount, price, null); }
    public CompletableFuture<Order> createLimitOrderWsAsync(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createLimitOrderWs(symbol, side, amount, price, params).thenApply(Order::new);
    }

    public Order createMarketOrder(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createMarketOrder(symbol, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createMarketOrder(String symbol, String side, Double amount) { return createMarketOrder(symbol, side, amount, null, null); }
    public CompletableFuture<Order> createMarketOrderAsync(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createMarketOrder(symbol, side, amount, price, params).thenApply(Order::new);
    }

    public Order createMarketOrderWs(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createMarketOrderWs(symbol, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createMarketOrderWs(String symbol, String side, Double amount) { return createMarketOrderWs(symbol, side, amount, null, null); }
    public CompletableFuture<Order> createMarketOrderWsAsync(String symbol, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createMarketOrderWs(symbol, side, amount, price, params).thenApply(Order::new);
    }

    public Order createLimitBuyOrder(String symbol, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createLimitBuyOrder(symbol, amount, price, params).join();
        return new Order(res);
    }
    public Order createLimitBuyOrder(String symbol, Double amount, Double price) { return createLimitBuyOrder(symbol, amount, price, null); }
    public CompletableFuture<Order> createLimitBuyOrderAsync(String symbol, Double amount, Double price, Map<String, Object> params) {
        return exchange.createLimitBuyOrder(symbol, amount, price, params).thenApply(Order::new);
    }

    public Order createLimitBuyOrderWs(String symbol, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createLimitBuyOrderWs(symbol, amount, price, params).join();
        return new Order(res);
    }
    public Order createLimitBuyOrderWs(String symbol, Double amount, Double price) { return createLimitBuyOrderWs(symbol, amount, price, null); }
    public CompletableFuture<Order> createLimitBuyOrderWsAsync(String symbol, Double amount, Double price, Map<String, Object> params) {
        return exchange.createLimitBuyOrderWs(symbol, amount, price, params).thenApply(Order::new);
    }

    public Order createLimitSellOrder(String symbol, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createLimitSellOrder(symbol, amount, price, params).join();
        return new Order(res);
    }
    public Order createLimitSellOrder(String symbol, Double amount, Double price) { return createLimitSellOrder(symbol, amount, price, null); }
    public CompletableFuture<Order> createLimitSellOrderAsync(String symbol, Double amount, Double price, Map<String, Object> params) {
        return exchange.createLimitSellOrder(symbol, amount, price, params).thenApply(Order::new);
    }

    public Order createLimitSellOrderWs(String symbol, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createLimitSellOrderWs(symbol, amount, price, params).join();
        return new Order(res);
    }
    public Order createLimitSellOrderWs(String symbol, Double amount, Double price) { return createLimitSellOrderWs(symbol, amount, price, null); }
    public CompletableFuture<Order> createLimitSellOrderWsAsync(String symbol, Double amount, Double price, Map<String, Object> params) {
        return exchange.createLimitSellOrderWs(symbol, amount, price, params).thenApply(Order::new);
    }

    public Order createMarketBuyOrder(String symbol, Double amount, Map<String, Object> params) {
        Object res = exchange.createMarketBuyOrder(symbol, amount, params).join();
        return new Order(res);
    }
    public Order createMarketBuyOrder(String symbol, Double amount) { return createMarketBuyOrder(symbol, amount, null); }
    public CompletableFuture<Order> createMarketBuyOrderAsync(String symbol, Double amount, Map<String, Object> params) {
        return exchange.createMarketBuyOrder(symbol, amount, params).thenApply(Order::new);
    }

    public Order createMarketBuyOrderWs(String symbol, Double amount, Map<String, Object> params) {
        Object res = exchange.createMarketBuyOrderWs(symbol, amount, params).join();
        return new Order(res);
    }
    public Order createMarketBuyOrderWs(String symbol, Double amount) { return createMarketBuyOrderWs(symbol, amount, null); }
    public CompletableFuture<Order> createMarketBuyOrderWsAsync(String symbol, Double amount, Map<String, Object> params) {
        return exchange.createMarketBuyOrderWs(symbol, amount, params).thenApply(Order::new);
    }

    public Order createMarketSellOrder(String symbol, Double amount, Map<String, Object> params) {
        Object res = exchange.createMarketSellOrder(symbol, amount, params).join();
        return new Order(res);
    }
    public Order createMarketSellOrder(String symbol, Double amount) { return createMarketSellOrder(symbol, amount, null); }
    public CompletableFuture<Order> createMarketSellOrderAsync(String symbol, Double amount, Map<String, Object> params) {
        return exchange.createMarketSellOrder(symbol, amount, params).thenApply(Order::new);
    }

    public Order createMarketSellOrderWs(String symbol, Double amount, Map<String, Object> params) {
        Object res = exchange.createMarketSellOrderWs(symbol, amount, params).join();
        return new Order(res);
    }
    public Order createMarketSellOrderWs(String symbol, Double amount) { return createMarketSellOrderWs(symbol, amount, null); }
    public CompletableFuture<Order> createMarketSellOrderWsAsync(String symbol, Double amount, Map<String, Object> params) {
        return exchange.createMarketSellOrderWs(symbol, amount, params).thenApply(Order::new);
    }

    public Order createPostOnlyOrder(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createPostOnlyOrder(symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createPostOnlyOrder(String symbol, String type, String side, Double amount) { return createPostOnlyOrder(symbol, type, side, amount, null, null); }
    public CompletableFuture<Order> createPostOnlyOrderAsync(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createPostOnlyOrder(symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order createPostOnlyOrderWs(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createPostOnlyOrderWs(symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createPostOnlyOrderWs(String symbol, String type, String side, Double amount) { return createPostOnlyOrderWs(symbol, type, side, amount, null, null); }
    public CompletableFuture<Order> createPostOnlyOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createPostOnlyOrderWs(symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order createReduceOnlyOrder(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createReduceOnlyOrder(symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createReduceOnlyOrder(String symbol, String type, String side, Double amount) { return createReduceOnlyOrder(symbol, type, side, amount, null, null); }
    public CompletableFuture<Order> createReduceOnlyOrderAsync(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createReduceOnlyOrder(symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order createReduceOnlyOrderWs(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        Object res = exchange.createReduceOnlyOrderWs(symbol, type, side, amount, price, params).join();
        return new Order(res);
    }
    public Order createReduceOnlyOrderWs(String symbol, String type, String side, Double amount) { return createReduceOnlyOrderWs(symbol, type, side, amount, null, null); }
    public CompletableFuture<Order> createReduceOnlyOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Map<String, Object> params) {
        return exchange.createReduceOnlyOrderWs(symbol, type, side, amount, price, params).thenApply(Order::new);
    }

    public Order createStopOrder(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createStopOrder(symbol, type, side, amount, price, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createStopOrder(String symbol, String type, String side, Double amount) { return createStopOrder(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createStopOrderAsync(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        return exchange.createStopOrder(symbol, type, side, amount, price, triggerPrice, params).thenApply(Order::new);
    }

    public Order createStopOrderWs(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createStopOrderWs(symbol, type, side, amount, price, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createStopOrderWs(String symbol, String type, String side, Double amount) { return createStopOrderWs(symbol, type, side, amount, null, null, null); }
    public CompletableFuture<Order> createStopOrderWsAsync(String symbol, String type, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        return exchange.createStopOrderWs(symbol, type, side, amount, price, triggerPrice, params).thenApply(Order::new);
    }

    public Order createStopLimitOrder(String symbol, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createStopLimitOrder(symbol, side, amount, price, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createStopLimitOrder(String symbol, String side, Double amount, Double price, Double triggerPrice) { return createStopLimitOrder(symbol, side, amount, price, triggerPrice, null); }
    public CompletableFuture<Order> createStopLimitOrderAsync(String symbol, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        return exchange.createStopLimitOrder(symbol, side, amount, price, triggerPrice, params).thenApply(Order::new);
    }

    public Order createStopLimitOrderWs(String symbol, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createStopLimitOrderWs(symbol, side, amount, price, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createStopLimitOrderWs(String symbol, String side, Double amount, Double price, Double triggerPrice) { return createStopLimitOrderWs(symbol, side, amount, price, triggerPrice, null); }
    public CompletableFuture<Order> createStopLimitOrderWsAsync(String symbol, String side, Double amount, Double price, Double triggerPrice, Map<String, Object> params) {
        return exchange.createStopLimitOrderWs(symbol, side, amount, price, triggerPrice, params).thenApply(Order::new);
    }

    public Order createStopMarketOrder(String symbol, String side, Double amount, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createStopMarketOrder(symbol, side, amount, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createStopMarketOrder(String symbol, String side, Double amount, Double triggerPrice) { return createStopMarketOrder(symbol, side, amount, triggerPrice, null); }
    public CompletableFuture<Order> createStopMarketOrderAsync(String symbol, String side, Double amount, Double triggerPrice, Map<String, Object> params) {
        return exchange.createStopMarketOrder(symbol, side, amount, triggerPrice, params).thenApply(Order::new);
    }

    public Order createStopMarketOrderWs(String symbol, String side, Double amount, Double triggerPrice, Map<String, Object> params) {
        Object res = exchange.createStopMarketOrderWs(symbol, side, amount, triggerPrice, params).join();
        return new Order(res);
    }
    public Order createStopMarketOrderWs(String symbol, String side, Double amount, Double triggerPrice) { return createStopMarketOrderWs(symbol, side, amount, triggerPrice, null); }
    public CompletableFuture<Order> createStopMarketOrderWsAsync(String symbol, String side, Double amount, Double triggerPrice, Map<String, Object> params) {
        return exchange.createStopMarketOrderWs(symbol, side, amount, triggerPrice, params).thenApply(Order::new);
    }

    public List<Order> createOrdersWs(Object orders, Map<String, Object> params) {
        Object res = exchange.createOrdersWs(orders, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> createOrdersWs(Object orders) { return createOrdersWs(orders, null); }
    public CompletableFuture<List<Order>> createOrdersWsAsync(Object orders, Map<String, Object> params) {
        return exchange.createOrdersWs(orders, params).thenApply(res -> toTypedList(res, Order::new));
    }

    public List<Order> fetchOrdersByStatusWs(String status, String symbol, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchOrdersByStatusWs(status, symbol, since, limit, params).join();
        return toTypedList(res, Order::new);
    }
    public List<Order> fetchOrdersByStatusWs(String status) { return fetchOrdersByStatusWs(status, null, null, null, null); }
    public CompletableFuture<List<Order>> fetchOrdersByStatusWsAsync(String status, String symbol, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchOrdersByStatusWs(status, symbol, since, limit, params).thenApply(res -> toTypedList(res, Order::new));
    }

    // ==========================================
    // Ledger
    // ==========================================

    public List<LedgerEntry> fetchLedger(String code, Long since, Long limit, Map<String, Object> params) {
        Object res = exchange.fetchLedger(code, since, limit, params).join();
        return toTypedList(res, LedgerEntry::new);
    }
    public List<LedgerEntry> fetchLedger() { return fetchLedger(null, null, null, null); }
    public CompletableFuture<List<LedgerEntry>> fetchLedgerAsync(String code, Long since, Long limit, Map<String, Object> params) {
        return exchange.fetchLedger(code, since, limit, params).thenApply(res -> toTypedList(res, LedgerEntry::new));
    }

    public LedgerEntry fetchLedgerEntry(String id, String code, Map<String, Object> params) {
        Object res = exchange.fetchLedgerEntry(id, code, params).join();
        return new LedgerEntry(res);
    }
    public LedgerEntry fetchLedgerEntry(String id) { return fetchLedgerEntry(id, null, null); }
    public CompletableFuture<LedgerEntry> fetchLedgerEntryAsync(String id, String code, Map<String, Object> params) {
        return exchange.fetchLedgerEntry(id, code, params).thenApply(LedgerEntry::new);
    }

    // ==========================================
    // Fees
    // ==========================================

    public TradingFees fetchTradingFees(Map<String, Object> params) {
        Object res = exchange.fetchTradingFees(params).join();
        return new TradingFees(res);
    }
    public TradingFees fetchTradingFees() { return fetchTradingFees(null); }
    public CompletableFuture<TradingFees> fetchTradingFeesAsync(Map<String, Object> params) {
        return exchange.fetchTradingFees(params).thenApply(TradingFees::new);
    }

    public TradingFees fetchTradingFeesWs(Map<String, Object> params) {
        Object res = exchange.fetchTradingFeesWs(params).join();
        return new TradingFees(res);
    }
    public TradingFees fetchTradingFeesWs() { return fetchTradingFeesWs(null); }
    public CompletableFuture<TradingFees> fetchTradingFeesWsAsync(Map<String, Object> params) {
        return exchange.fetchTradingFeesWs(params).thenApply(TradingFees::new);
    }

    public TradingFeeInterface fetchTradingFee(String symbol, Map<String, Object> params) {
        Object res = exchange.fetchTradingFee(symbol, params).join();
        return new TradingFeeInterface(res);
    }
    public TradingFeeInterface fetchTradingFee(String symbol) { return fetchTradingFee(symbol, null); }
    public CompletableFuture<TradingFeeInterface> fetchTradingFeeAsync(String symbol, Map<String, Object> params) {
        return exchange.fetchTradingFee(symbol, params).thenApply(TradingFeeInterface::new);
    }

}
