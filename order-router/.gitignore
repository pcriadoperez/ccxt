node_modules/
dist/

# API keys: digests only, but still not something to commit.
keys.json
data/

# Ad-hoc measurement scripts. Seventeen of these were committed during the memory and IPC
# investigations; the findings belong in commit messages and docs, the throwaway harnesses do not.
.h3tmp/
.tmp-h1/
*probe*.mjs
h4-measure-*.mjs
