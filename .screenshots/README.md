# Rendered screenshots — "How fast is CCXT in each of its six languages?"

Images-only branch. It exists so the benchmark PR can embed screenshots of the
rendered blog post without putting multi-megabyte PNGs into the PR diff.

Captured from `next dev` at a 1024px viewport, 2× DPR, from
`website/content/blog/ccxt-performance-across-languages.mdx` on branch
`claude/serene-thompson-kfhzh8`.
